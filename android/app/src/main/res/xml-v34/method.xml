<?xml version="1.0" encoding="utf-8"?>
<input-method xmlns:android="http://schemas.android.com/apk/res/android"
    android:supportsSwitchingToNextInputMethod="true">
    <!-- API 34+: keep both subtypes (never collapse). Keyboard still overrides
         so VocaPhone stays a normal IME. Voice omits
         overridesImplicitlyEnabledSubtype so AOSP can show voice_input_label
         instead of a null subtypeLabel (#272). HeliBoard enablement is
         VoiceShortcutIme.publishEnabledSubtypes from Application/IME via
         setExplicitlyEnabledInputMethodSubtypes. Long-press may still list
         two rows once voice is enabled; they should no longer be identical. -->
    <!-- Non-auxiliary keyboard subtype keeps VocaPhone selectable as a normal IME.
         An auxiliary-only method.xml would mark the whole IME as aux and hide it
         from the default keyboard picker. -->
    <subtype
        android:label="@string/ime_name"
        android:imeSubtypeMode="keyboard"
        android:isAsciiCapable="true"
        android:overridesImplicitlyEnabledSubtype="true" />
    <!-- isAuxiliary keeps it a one-shot voice shortcut, not a picker language.
         Do not set overridesImplicitlyEnabledSubtype on voice. -->
    <subtype
        android:label="@string/voice_input_label"
        android:imeSubtypeMode="voice"
        android:isAuxiliary="true" />
</input-method>
