<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Only the window chrome comes from here; everything the user sees is drawn
      by Compose, which follows the system dark-mode setting on its own.
    -->
    <style name="Theme.VocaPhone" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!-- One frame, no animation, nothing drawn: only used to start the service. -->
    <style name="Theme.VocaPhone.Invisible" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
